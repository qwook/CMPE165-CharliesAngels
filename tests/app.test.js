
import { chai } from 'meteor/practicalmeteor:chai';


// if (Meteor.isClient) {
  describe('register', function () {
    it('can go to the registration page.', function () {
      throw new Error("Couldn't register!");
    });
  });
// }
